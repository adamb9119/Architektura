{{ form_start(form, {attr: {novalidate: 'novalidate'} }) }}
    {{ form_row(form.title) }}
    {{ form_row(form.type) }}
    {{ form_row(form.number) }}
    {{ form_row(form.page) }}
    {{ form_row(form.add)}}
{{ form_end(form) }}